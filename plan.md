# 実験計画

## 実験1: モデル同定（ステップ電流応答）

### 目的

電流入力 $I$ [mA] に対する角速度出力 $\omega$ [RPM] の伝達関数を同定する。

### システムモデル

モーター＋負荷系を1次系と仮定する。

$$\frac{\omega(s)}{I(s)} = \frac{K}{\tau s + 1}$$

| パラメータ  | 意味                              | 単位   |
| ----------- | ------                            | ------ |
| $K$         | DCゲイン（電流→速度の定常ゲイン） | RPM/mA |
| $\tau$      | 時定数                            | s      |

物理的には、モーターの運動方程式 $J\dot{\omega} = K_t I - B\omega$ より：

- $K = K_t / B$（電流-トルク係数 $K_t$ と粘性摩擦係数 $B$ の比）
- $\tau = J / B$（慣性モーメント $J$ と粘性摩擦係数 $B$ の比）

> $K_t$ と $B$ を個別に求めるには別途実験が必要。まず $K$、$\tau$ を同定する。

### ステップ応答の理論値

ステップ電流 $I_{\text{step}}$ を加えたとき：

$$\omega(t) = K I_{\text{step}} \left(1 - e^{-t/\tau}\right)$$

- 定常値: $\omega_{ss} = K \cdot I_{\text{step}}$
- $t = \tau$ のとき $\omega = 0.632 \cdot \omega_{ss}$

### 同定手順

摩擦等の非線形要素の影響を除くため、静止状態からではなく平衡回転状態からステップを加える。
計測は最初から行い、ステップ前後を1本のCSVデータとして取得する。

1. PCからコマンドを受信し、計測開始
2. $I_{\text{pre}}$ を印加しながら $T_{\text{hold}}$ 秒間計測（平衡状態の確認）
3. $I_{\text{pre}} \to I_{\text{step}}$ へ切り替え（ステップ印加）
4. さらに $T_{\text{hold}}$ 秒間計測（ステップ応答の記録）
5. 計測終了、モーター停止

合計 $2 T_{\text{hold}}$ msのCSVデータにステップ前後が含まれる（例: $T_{\text{hold}}=5000$ ms → 計10秒）。

### パラメータの算出

| パラメータ           | 算出方法                                                   |
| -----------          | ---------                                                  |
| 定常値 $\omega_{ss}$ | 収束後の $\omega$ の平均値                                 |
| DCゲイン $K$         | $K = \omega_{ss} / I_{\text{step}}$                        |
| 時定数 $\tau$        | $\omega(t^*) = 0.632\,\omega_{ss}$ となる時刻 $t^*$ を読む |

### 実装方針

- ステップ電流値はシリアル通信でPCから送信する（コード再書き込み不要）
- PC から数値（mA）を送信 → AtomS3 がステップ印加 → CSV データを返送
- `setCurrent(I_step * 100)` でステップ印加
- `getSpeedReadback() / 100.0f` で速度取得（単位: RPM）
- シリアルに `時刻[ms], 速度[RPM]` のCSV形式で出力 → PC側でプロット

#### シリアルコマンド仕様（案）

| PCからの送信            | AtomS3の動作                                                                              |
| -------------           | -------------                                                                             |
| `200 300 5000` + 改行   | $I_{\text{pre}}=200$ mA で5000 ms計測、$I_{\text{step}}=300$ mA へステップして5000 ms計測 |
| `-200 -300 3000` + 改行 | 逆転方向・3000 msずつで同様に動作                                                         |

出力フォーマット（CSV）:
```
time_ms,speed_rpm,current_ma
0,150.3,200.1
20,151.0,199.8
...
5000,151.2,300.0   ← ここでステップ
5020,163.4,299.9
...
```

---

## 実験2: （未定）

同定したモデルをもとにフィードバック制御系を設計・実装する予定。
